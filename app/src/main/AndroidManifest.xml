<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.RobX.MyHealth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:configChanges="locale|orientation"
        android:theme="@style/Theme.MyHealth">
        <activity android:name=".SelectLanguageActivity"></activity>
        <activity android:name=".RatingActivity" />
        <activity android:name=".PandemicMapActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ArticleTextActivity" />
        <activity android:name=".TravelAdviceWebViewActivity" />
        <activity android:name=".TravelAdviceActivity" />
        <activity android:name=".SelfAssessmentActivity" />
        <activity android:name=".FAQActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".SelectMCActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>